spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate.dialect: org.hibernate.dialect.H2Dialect

  flyway:
    enabled: false

  exchange:
    rate:
      api:
        url: "https://openexchangerates.org/api/latest.json?app_id=ea2968816b464fd786e10b7e47d85911"

